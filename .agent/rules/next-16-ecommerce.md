---
trigger: always_on
---

use shadcn ui components and tailwind for layout and styling and build components
use zustand for state management
use css for component styling
read color variables from global.css and if color not matched usetailwind else define the color
