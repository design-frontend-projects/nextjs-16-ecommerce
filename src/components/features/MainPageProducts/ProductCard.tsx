import React from 'react';
import { CheckCircle, PhoneCallIcon } from 'lucide-react';
type Props = {
  stockCheck?: boolean;
};

const ProductCard = ({ stockCheck = false }: Props) => {
  return <div>ProductCard</div>;
};

export default ProductCard;
